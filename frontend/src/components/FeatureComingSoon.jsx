export function FeatureComingSoon({ label }) {
  return (
    <button type="button" className="btn btn-ghost" disabled title="Recurso previsto no ERSW, ainda não implementado">
      {label} (em breve)
    </button>
  );
}
